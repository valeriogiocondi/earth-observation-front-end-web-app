type Style = {

    name: string | null,
    title: string | null,
    legendUrl: string | null,
    legendFormat: string | null,
    width: string | null,
    height: string | null,
};

export default Style;